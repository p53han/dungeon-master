# Progress

## What Exists

- Raw source note exists at `memory-bank/Trying to find a TTRPG community on Discord that i.md`.
- Core Memory Bank files exist and stay current with the architecture:
  - `projectbrief.md`
  - `productContext.md`
  - `systemPatterns.md`
  - `techContext.md`
  - `activeContext.md`
  - `progress.md`
- Backend code under `src/dungeon_master/`:
  - `api.py` — FastAPI surface (state, oracle, action, reset endpoints).
  - `cli.py` — `dungeon-master` console script that runs uvicorn.
  - `campaign.py` — split character-generation + campaign-generation layer.
  - `models.py` — Pydantic state/event schemas plus campaign lifecycle + richer character sheet.
  - `oracle.py` — deterministic oracle engine.
  - `turn_router.py` — conservative natural-language router for `/api/turn`.
  - `state_store.py` — JSON state, JSONL event log, timestamped checkpoints, and named turn checkpoints for regeneration.
  - `narrative.py` — LiteLLM narrative client with retries, Kimi/OpenRouter defaults, and fallback narration.
  - `service.py` — application service coordinating oracle, state, and narration.
  - `settings.py` — environment-backed state path configuration.
- Frontend code under `web/`:
  - `package.json`, `tsconfig.json`, `vite.config.ts`, `svelte.config.js`, `index.html`.
  - `src/main.ts`, `src/App.svelte` (setup-aware layout: CharacterSetup before campaign start, then StatusStrip → CharacterFolio + ChatFeed → Composer → Inspector).
  - `src/lib/api.ts`, `src/lib/types.ts`, `src/lib/store.svelte.ts` (runes-based store).
  - `src/lib/slash.ts` + `slash.test.ts` (slash-command parser with Vitest coverage).
  - `src/lib/character.ts` + `character.test.ts` + `message-actions.ts` (setup + regenerate helpers).
  - `src/lib/quiz.ts` + `quiz.test.ts` (assist-mode interview helpers: answer state, validation, payload builder).
  - `src/styles/app.css` (bespoke pigment palette + three-voice type system + parchment/iron surfaces).
  - `public/fonts/alagard.ttf` (CC-BY pixel font; engine voice).
  - `src/components/`: `CharacterSetup`, `CharacterTemplateCard`, `CharacterEditor`, `LoadingPanel`, `StatusStrip`, `CharacterFolio`, `ChatFeed`, `ChatMessage`, `MessageActions`, `MechanicalReceipt`, `Composer`, `Inspector`, `ChaosDial`, `Drawer`, `ThreadsPanel`, `NPCsPanel`, `NotesEditor`.
- Tests under `tests/` cover oracle, state store, narrative, service, and the FastAPI surface.
- Frontend tests under `web/src/lib/` use Vitest. Coverage now spans the slash parser, character helpers, message regeneration gate, the assist-mode quiz builders, the Cairn formatting helpers (`web/src/lib/cairn.test.ts`), and the server-side cancel POST contract (`web/src/lib/cancel.test.ts`).
- Manual testing notes exist at `docs/manual-testing.md` (product walkthrough + short developer-knobs appendix).
- Project setup exists in `pyproject.toml`, `.env.example`, `.gitignore`, and `README.md`.
- A persistent feature backlog now exists at `memory-bank/featureKanban.md`, organized as a Kanban-style ticket board for staged planning.

## What Has Been Decided

- The project direction is an AI-assisted game-master application.
- Canonical state stays outside the LLM.
- State updates are validated, persisted, and checkpointed.
- The project started oracle-first (Mythic-like deterministic backbone), but the next approved direction is a Cairn 2e-inspired rules layer built on top of that deterministic foundation.
- The LLM narrative engine must not roll dice, change chaos factor, or mutate state.
- Campaign opening content and oracle word banks are generated by the model at campaign initialization rather than hardcoded in domain defaults.
- The app uses LiteLLM for model switching, defaulting to OpenRouter Kimi K2.6 with task-based medium/high reasoning and deterministic fallback when no model/key is available.
- The UI is a bespoke Svelte 5 + Vite + TS frontend talking to a FastAPI backend; Streamlit was removed.
- The UI is chat-first: before play, a required character-creation flow chooses/templates/edits the protagonist; during play, character/inventory lives in a persistent left folio, mechanics live in a compact peek-on-demand inspector drawer with collapsible per-message receipts, slash commands (`/ask`, `/event`, `/scene`, `/chaos`, `/reset`, `/help`) are the explicit oracle entry points, and bare natural language goes to backend `/api/turn` for conservative automatic routing.
- Regenerate is now checkpoint-backed and preserves deterministic oracle outcomes; latest-response only. The client `Stop` button now triggers true server-side cancellation (`POST /api/requests/{id}/cancel`) before aborting the local fetch, so the LLM stops spending tokens on a turn the player abandoned.
- The backend now has a real cancellation path for streamed requests: `request_id`s are registered server-side, `POST /api/requests/{request_id}/cancel` flips a cooperative token, LiteLLM stream readers stop on cancellation, and streamed state mutations are discard-only until final commit.
- The app now has a real tiered memory sidecar. Committed canon is compacted into `data/memory.json`, and bounded planner/narrator memory context is drawn from that sidecar rather than replaying raw logs into prompts.
- The memory retrieval layer should avoid hardcoded lexical heuristics for semantic meaning. The implemented pass uses canonical references already present in outcomes/state, scene locality, active status, and recency ordering rather than stopword/token-overlap scoring.
- The first bug-fix pass on the memory/regenerate/planner failure issues is now implemented: streamed turns checkpoint pre-narration state, turn checkpoints persist `execution_context`, memory rebuilds use exact turn-checkpoint metadata, planner failure now surfaces explicitly at the API boundary, and setup-stream server aborts now surface as frontend errors instead of silent `null`s.
- Pixel font Alagard is the engine's visual voice (chaos numeral, dice, oracle tags, kicker labels, system messages); Cormorant Garamond stays the narrative voice; IM Fell English SC stays the display voice.
- Vanilla CSS (not Tailwind) is used so the "Oracle's Ledger" aesthetic is not homogenized.
- `.env` knobs are treated as developer / debug controls only; if a switch ever feels like it should be in the UI, that's a signal to promote it to a real control.
- The HTTP surface always returns the entire `GameState` so the frontend never reconciles partial diffs.
- The backend binds only to `127.0.0.1`; this is a personal-machine app, not for hosting.
- The chosen next mechanical direction is a **Cairn 2e-inspired dark-fantasy adaptation** rather than a pure oracle-only loop.
- The Cairn-inspired layer should stay **more structured than not**: canonical stats, HP, burden/slots, and deterministic item/mechanical effects, all kept outside the LLM.
- Existing authored characters should be preserved; the current authored character now has a one-time **LLM-backed** backend backfill path into nested Cairn mechanics state, and future mechanics-aware characters can bypass that path by submitting explicit mechanics.
- The frontend now surfaces the Cairn mechanical state **read-only**: the persistent left folio renders HP / STR / DEX / WIL, a segmented burden meter, fatigue, status chips (deprived, critically wounded, doomed, paralyzed, delirious, dead, overloaded), the LLM-derived skills/abilities lists, and per-item tag chips plus weapon-die / armor-bonus / uses / slots readouts. Equipped and primary-weapon badges sit on inventory items. The inspector exposes a collapsed "Cairn build notes" drawer showing the LLM's backfill rationale. The receipt strip recognizes the new `save` / `attack` / `harm` / `recovery` outcome kinds and renders structured Cairn fields (ability, target, success, damage-after-armor, hp/str/fatigue deltas, scar, rest kind, burden) in the body.
- The folio rail is a true two-column layout on most desktop windows: the rail width is `clamp(296px, 40vw, 720px)` so it grows aggressively into the gutter past the centered chat's 72ch cap, and `CharacterFolio` introduces an inner `.folio__layout` grid (the container query target) plus two `.folio__col` flex columns. The outer `.folio` element carries `container-type: inline-size` / `container-name: folio`; the inner layout exists because container queries can only restyle descendants of the container, not the container element itself. At `@container folio (min-width: 280px)` the rail flips from a 4-row single column into an identity (plate + condition / drive) on the left, ledger (mechanics readout + inventory) on the right. In two-column mode the condition / drive line clamp is dropped because the section now owns enough vertical room to show full text; the narrow fallback keeps the 3-line clamp so the inventory section doesn't get pushed offscreen. A vertical rule between the columns replaces the inter-section horizontal rules to avoid stranded lines above the shorter column's empty space.
- Backend streaming/persisted-thinking is now wired end-to-end for **all** model-backed outputs, not just active play. Character templates, setup quiz generation, scratch/template drafts, quizzed drafts, campaign start, player turns, generic actions, and narrative regeneration all share one NDJSON contract: a leading `meta` event with `request_id` + `route`, then `thinking_delta` / `content_delta` chunks, then a terminal `final_state` (state-mutating endpoints) or `final_payload` (setup endpoints, with a `kind` discriminator for `character_quiz` vs `character_draft`), or a terminal `error` event for backend-authored failures. Final thinking is preserved either in the setup response payload or on a persisted `GameEvent` when a `GameState` is committed, and is also surfaced on `final_state.thinking` for clients that don't want to re-read `state.action_log[-1].thinking`.
- Frontend pass complete. `web/src/lib/api.ts` exposes streamed siblings of every model-authored unary endpoint; `web/src/lib/store.svelte.ts` consumes them through `#runStreaming` (state mutations, with optional roll tumble between `mechanics_ready` and the first `content_delta`) and `#runStreamingPayload` (typed setup artifacts), each with a transparent fallback to the unary endpoint on a 404/405. ChatFeed renders a provisional DM bubble bound to `streaming.content` / `streaming.thinking` / `streaming.pendingOutcome`. The Inspector renders a default-open `Combat` drawer when an encounter is active, and `web/src/lib/combat.ts` adapts the canonical backend `EncounterState` (`state.encounter`) to the tracker's richer per-foe display shape (`status` derived from defeated/fled/critically_wounded; `morale_triggered` from the encounter's casualty / half-force flags).

## What Remains To Decide

- Whether to keep or replace the original oracle tables (model-generated word banks may be enough).
- Whether to add a formal graph/orchestration framework later.
- Optional UI field for user-authored campaign seed prompts before generation.
- Optional in-app controls for model/provider switching.
- Checkpoint browse + rollback UI.
- Thread and NPC editing UI.
- Exact scope of the Cairn adaptation: how close to Cairn 2e text/structure vs. how far into a custom dark-fantasy rules hack.
- How much of Cairn combat/actions should be inferred from free text versus left to explicit frontend controls and explicit API calls.
- How to present the now-available streamed reasoning/thinking in the frontend without overwhelming the chat-first UX (collapsed transcript block, side panel, inline disclosure, etc.).
- Whether the current bounded `data/memory.json` retrieval is sufficient over long campaigns or should be upgraded to a richer semantic reranker / vector-backed layer later.
- Whether to TOON-encode prompt-input slices of `GameState` (inventory, NPCs, threads, oracle banks, encounter combatants) as a token-savings layer in front of the LLM. Earmarked, not adopted. Decision criteria are recorded under "Open" in `activeContext.md`.

## Suggested Implementation Milestones

1. ~~Live OpenRouter smoke against the real model.~~ **DONE** — see "Live LLM playability" entry below; routine narration turns land in ~2-3 minutes and combat turns in ~3-5 minutes with full streaming of thinking + content.
2. ~~Add a true tiered memory / compaction system.~~ **DONE** — `src/dungeon_master/memory.py` + `data/memory.json` now provide compacted GM notes and bounded retrieval for planner/narrator prompts.
3. Optional enhancement: emit `mechanics_ready` mid-stream (before the first `content_delta`) so the receipt strip pins early. Frontend already handles the event.
4. Cairn explicit-controls pass: bind `/api/cairn/attack`, `/cairn/harm`, `/cairn/recover`, `/cairn/equip` to deliberate UI affordances (equip toggles in the folio, attack/harm/recover controls in the inspector or a combat tray).
5. Parallelize the TurnRouter classifier and encounter-seeding LLM calls — currently they run sequentially in front of narration, which is the dominant latency cost of a combat turn. Both are independent of each other and of the narration prompt.
6. Decide which explicit backend mechanics routes become slash commands, which become form controls, and which stay hidden behind richer UI affordances.
7. Add UI controls for managing threads and NPCs in the Inspector.
8. Add checkpoint browsing and rollback in the Inspector.
9. Add richer event focus/reference display in the receipt (link an outcome's `referenced_thread_id` / `referenced_npc_id` to the relevant inspector entry).
10. After the explicit-controls pass lands, refactor persistence toward multiple character saves with their own campaigns/checkpoints/event logs.

## Known Issues and Risks

- The raw note contains exploratory language and assumptions that should be distilled carefully rather than copied verbatim into user-facing product copy.
- The oracle/rules layer should be modular from the start.
- Proprietary table content should not be copied into code unless licensing is handled.
- Browser testing observed expected 404s from the default local Ollama-compatible endpoint when no model server is running; the app handled this with fallback narration.
- The frontend now renders Cairn mechanics state read-only. It does not yet call the explicit Cairn mutation endpoints (`/api/cairn/attack`, `/harm`, `/recover`, `/equip`); those stay live but unbound until the explicit-controls pass.
- Live LLM playability: Kimi K2.6 reasoning latency dominates per-turn time. Routine narration is ~2-3 minutes, combat (with new encounter seeding) is ~3-5 minutes. Two pitfalls were identified and fixed empirically — `response_format={"type":"json_object"}` causes Kimi to over-deliberate (335s vs ~30s without), and `reasoning.effort` and `reasoning.max_tokens` are mutually exclusive on OpenRouter. We now pass only `reasoning.max_tokens` (per-task budgets: 600 router / 1200 encounter / 1500 routine narration / 3000 scene+random) and feed JSON output through `narrative.extract_json_object` (strips ```json fences, isolates first balanced `{...}`).
- The frontend `Stop` button now hits the backend cancel endpoint before aborting the local fetch (`web/src/lib/api.ts::cancelRequest`, wired via `store.cancelCurrentRequest`). Manual smoke against the live FastAPI server is still pending; the cancel POST contract is covered by `web/src/lib/cancel.test.ts`.
- The new memory layer is intentionally conservative about semantic selection. It now avoids hardcoded stopword/token heuristics, but may still want a richer semantic reranker later if long campaigns show recall gaps the current canonical/recency-bounded selection cannot solve.
- The persistence layer is still file-based and single-user. The bug-fix pass hardened atomic writes for checkpoints and turn checkpoints and stopped some forms of drift, but a full cross-file transaction boundary still does not exist in the same way a database transaction would.

## Current Status

The chat-first pivot is complete and now sits behind a real pre-campaign setup phase. Character creation supports templates, scratch editing, and a multi-step AI-assisted **interview** path (concept → tailored quiz → review → draft). All generation phases show a `LoadingPanel` with a same-place cancel button. `StatusStrip`, `CharacterFolio`, `ChatFeed`, `ChatMessage`, `MessageActions`, `MechanicalReceipt`, `Composer`, and compact `Inspector` ship as the active-play shell. Alagard pixel font wired in with a VT323 fallback. Slash-command parser unit-tested. Backend `/api/turn` uses LLM-backed structured classification instead of regex routing. `GameState.character` provides both structured narrative character data and nested Cairn mechanics data, with a one-time LLM-backed backfill path for the current authored character.

The backend streaming pass is now also complete. Setup generation no longer lives on a special blocking protocol: templates, quiz, drafts, and campaign start now mirror gameplay with streamed deltas plus final typed payloads. Persisted thinking now survives not just turn/regenerate streams, but every model-backed backend path where there is a canonical place to store it.

The backend cancellation pass is now also complete. Every streamed backend route emits a stable request ID, can be cancelled via `POST /api/requests/{request_id}/cancel`, threads that cancellation token through route classification / setup generation / encounter seeding / narration, and defers streamed persistence until commit so cancelled turns leave `game_state.json`, `events.jsonl`, and streamed turn checkpoints untouched.

The first Cairn frontend pass is now also complete (read-only). The persistent folio renders HP/STR/DEX/WIL, a segmented burden meter, status chips (priority-ordered), skills/abilities, and per-item tag/weapon-die/armor/uses readouts including equipped + primary-weapon badges. `MechanicalReceipt` is exhaustive over the new `save | attack | harm | recovery` kinds, surfacing structured Cairn fields (ability, target, success, base/post-armor damage, hp/str/fatigue deltas, scar, rest kind, burden) in the expanded body. Inspector adds a collapsed "Cairn build notes" drawer that displays the LLM-authored backfill rationale. The character editor renders the read-only Cairn block above the parchment editor when a draft already has derived mechanics, and stays suppressed while the draft's `cairn.source === "unset"`.

The narration prompt has now been tightened after reviewing live reasoning traces. `src/dungeon_master/narrative.py` asks for `1-2 compact paragraphs, usually 1` instead of `1-3`, explicitly tells the narrator to mirror the player's declared action before extending the scene, and forbids turning flavor / latent threats into hardened present-tense facts unless the oracle outcome or canonical state supports them. The goal is to keep the stylish dark-fantasy voice while reducing branch-generation, over-escalation, and "novel mode."

The backend turn planner refactor is now also complete. `src/dungeon_master/turn_router.py` no longer only emits a single route label; it now returns a bounded `TurnPlan` with 1-3 ordered ops from a fixed vocabulary. The legacy `route` is retained for compatibility, but `GameService` now executes the plan in order, allowing prep/state-adjacent steps like inventory inspection, scene searching, item use, item dropping, and equipment changes before one primary oracle/mechanical resolution. `NarrativeEngine` now receives an executed-step summary so the prose describes what Python actually did instead of inferring everything back from one coarse route label.

The tiered memory pass is now also complete. `src/dungeon_master/memory.py` defines the compacted GM-note sidecar and retrieval contexts, `StateStore` persists `data/memory.json`, `GameService` refreshes it after committed saves, and both `TurnRouter` and `NarrativeEngine` now accept bounded memory-context blocks. This gives the app "GM notes" continuity without replaying the full transcript or feeding model reasoning traces back into future prompts.

The first post-memory bug-fix pass is now also complete. `GameService` now rebuilds memory from exact committed-turn metadata rather than lossy `action_log` indexing, regenerate uses the same memory/execution context inputs as the original turn, streamed turns checkpoint true pre-narration state, `TurnRouter` raises an explicit planning failure when structured routing collapses, and the setup-stream store path now surfaces server-side aborts as errors.

The product backlog triage pass is also now complete. The user clarified that LLM free-text inference remains the intended primary mechanism, but the game still needs a queue of omitted player-facing features. Those are now captured in `memory-bank/featureKanban.md`, with current emphasis on retreat/disengage, inventory progression, dynamic thread/NPC updates, enemy-first/ambush combat handling, campaign-end flow, common actions tray, slash suggestion UI, and long-session history browsing. Save slots, rollback UI, and manual correction/editing are explicitly deferred.

The backend half of `F-01 Retreat / Disengage` is now complete. The combat model gained canonical retreat outcomes (`caught`, `disengaged`, `escaped`), encounter state now records pursuit/disengaged/end-reason information, `TurnRouter` can classify retreat, `GameService` executes it, and `src/dungeon_master/api.py` exposes `/api/cairn/retreat`. A small compatibility patch also landed on the frontend type/receipt layer so a retreat outcome kind renders coherently.

`F-01 Retreat / Disengage` is now fully shipped. The frontend pass added a `/retreat [reason]` slash command (with `/flee` and `/disengage` aliases), translated through the regular turn pipeline so the planner classifies it and narration + memory run uniformly. The composer now also surfaces a slash suggestion menu (Up/Down to navigate, Tab/Enter to complete, Esc to dismiss) backed by a single `SLASH_COMMANDS` descriptor list shared with `/help`, and the combat tracker carries a small read-only "Type `/retreat` to attempt to disengage" hint when an encounter is active. The explicit `/api/cairn/retreat` endpoint is retained as a deterministic-only path for future callers but is not what the slash command targets — the chat-first invariant keeps retreats inside the same narrate-and-checkpoint pipeline as every other turn.

Verification completed for the frontend half of `F-01`:

- `npm run check` (svelte-check) — 0 errors, 0 warnings
- `npm test` (Vitest) — 8 files, 94 tests passing
- `npm run build` — clean production build

Verification completed:

- `uv run ruff check src tests`
- `uv run mypy src tests`
- `uv run pytest -q` (full backend suite passing, including new cancellation coverage for the API endpoint, narrative stream helper, and discard-on-cancel service path)
- `uv run pytest tests/test_memory.py tests/test_state_store.py tests/test_narrative.py tests/test_turn_router.py tests/test_service.py tests/test_api.py -q` (focused memory + prompt-wiring pass green)
- `uv run pytest tests/test_service.py tests/test_api.py tests/test_turn_router.py tests/test_narrative.py tests/test_memory.py tests/test_state_store.py -q` (focused bug-fix pass green — covers streamed regenerate without duplicate narrative, explicit planning-failure surfacing, checkpoint `execution_context` persistence, query-aware planner memory, and compacted narrator prompt payloads)
- `uv run pytest tests/test_cairn.py tests/test_service.py tests/test_api.py tests/test_turn_router.py -q` (focused retreat pass green — covers escaped/disengaged/caught retreat outcomes, planner routing, service plumbing, and the explicit `/api/cairn/retreat` route)
- `uv run pytest tests/test_narrative.py -q` (6 passing — includes a focused prompt-contract test asserting the narrator now requests compact, action-mirroring, fact-grounded prose)
- `uv run pytest tests/test_turn_router.py tests/test_service.py tests/test_api.py tests/test_narrative.py -q` (planner-focused pass green — includes a router test for a compound equip+attack plan and a service test for a compound inventory-inspect + drop-item plan)
- Live backend HTTP smoke against the running FastAPI server: `POST /api/action/stream` emitted `meta` with a real `request_id`, `POST /api/requests/{request_id}/cancel` returned `{"cancelled": true}`, the stream closed without any terminal events, and a follow-up `GET /api/state` confirmed `action_log` / `oracle_history` were unchanged.
- Live browser smoke via `cursor-ide-browser` after the memory pass: the app accepted a new player submission, entered streaming, exposed `Stop response`, and returned cleanly to idle after cancellation without UI breakage.
- Isolated commit-path smoke on a fallback backend (`OPENROUTER_API_KEY=\"\"`, temp state path): a deterministic `/api/oracle/yes-no` turn wrote `/tmp/dm-memory-fallback/memory.json`, and the sidecar reflected `turn_count == 1`, the committed `yes_no` summary, and a compacted current-scene summary.
- Live OpenRouter smoke against the real Kimi K2.6 model: routine narration ("I move quietly along the chapel wall...") landed in 138s (1499 thinking deltas + 355 content deltas, 1509 chars of prose); a combat-triggering attack ("I lunge at the lead leper with my notched cudgel...") landed in 305s, seeded a real encounter with three hand-stat'd foes (Lead Leper / Ash-Bound Leper / Myrrh-Choked Leper), executed three full Cairn rounds, and returned narration that referenced both the foes and the dice/HP outcomes. After verification, `data/game_state.json` was rolled back to the campaign-init checkpoint so play starts clean (HP 3/3, no encounter, only the `Campaign initialized` event present); pre-rollback copies live in `data/backups/`.
- `cd web && npm run check` (0 errors / 0 warnings)
- `cd web && npm test -- --run` (81 passing — adds the NDJSON `consumeStream` parser tests covering boundary-spanning chunks, abort semantics, malformed input, and the typed event dispatch; the `combatFromState` adapter tests; and the new server-side cancel POST contract tests in `web/src/lib/cancel.test.ts` covering URL shape, path-encoding of unsafe characters, and the stale/already-finished `{cancelled: false}` response)
- `cd web && npm test -- --run` (82 passing after the bug-fix pass — adds a store-level regression test ensuring setup streams that end as `{ kind: "aborted", reason: "server" }` become a visible error instead of silently returning `null`)
- `cd web && npm run build` (clean production build)
- `cd web && npm run build` (clean production build)
- `cd web && npm run check` + `npm test -- --run` after the retreat pass (clean — includes minimal frontend compatibility for the new `retreat` outcome kind)
- Live browser smoke via `cursor-ide-browser`: existing `Vrtanes` save was loaded, folio rendered the full Cairn readout (STR 5/5, DEX 10/10, WIL 12/12, skills + abilities, equipped/primary badges, weapon die / uses / slots), expanding a `Save · DEX · failed` receipt revealed the d20 roll, and the inspector's "Cairn build notes" drawer was collapsed by default and revealed the LLM's backfill rationale on click.
- Live OpenRouter/Kimi end-to-end with concept "Armenian Apostolic paladin who rejects in-world magic as base paganism, but uses prayer and piety to defeat foes": the quiz endpoint returned 5 deeply tailored questions ("What mark has your refusal of all sorcery left upon your flesh?", "What holy thing did you destroy to prove your faith had no equal?", etc.) with options grounded in the concept. Submitting answers + a final note ("Speaks Grabar under his breath when frightened") produced a draft named "Sahak / Apostolic Penitent" whose backstory, drive, flaw, condition, and four inventory items every traced back to specific answers and the final note.
- Confirmed the previous "draft is generic boilerplate" behavior was caused by Kimi K2.6 Thinking always burning ~2-3k reasoning tokens regardless of the requested `effort`, leaving 0 content tokens at the old 2200-token cap. All character / quiz / draft endpoints now use `medium` (or `low` for the quiz) reasoning + `max_tokens=12000`, and every fallback path now logs via `logger.exception` so silent failures appear in the uvicorn console.
- Backend-only Cairn pass complete: `src/dungeon_master/cairn.py` now owns deterministic save resolution, outgoing attacks, incoming harm/critical damage/scars, recovery, equipment toggles, one-time **LLM-backed** narrative-to-mechanics backfill, and derived armor/burden recomputation. New FastAPI routes expose those mechanics directly for the frontend pass.
- Character generation/backfill inventory philosophy corrected: interview/body-horror flavor now belongs mainly in condition, stats, and short textual skills/abilities; generated loadouts should be practical bundles with at most one or two signature biography-derived items, not literalized symptom props.

Known tooling note:

- Pinchtab's `browser_fill` and `browser_type` set the DOM `value` attribute but don't always dispatch the `input` event Svelte 5's `bind:value` listens for; manual smoke tests need a real keyboard. The slash-parser Vitest suite + the FastAPI integration tests cover the routing contract independently.
