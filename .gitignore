.DS_Store
.env
.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

# Persisted game state — regenerated on every campaign reset
data/game_state.json
data/events.jsonl
data/memory.json
data/checkpoints/
data/turn-checkpoints/
data/backups/
# F-12 multi-save layout: per-save runtime state.
# `library.json` is the active-save manifest, `saves/<save_id>/` is
# a full StateStore root per campaign. None of this is source code;
# it's regenerated as the user plays.
data/library.json
data/saves/

# Frontend
web/node_modules/
web/dist/
web/.svelte-kit/
